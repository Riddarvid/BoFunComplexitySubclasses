module Subclasses.Monotone where

-- Wikipedia:
-- a Boolean function is monotonic if, for every combination of inputs, switching one
-- of the inputs from false to true can only cause the output to switch from false to true
-- and not from true to false

